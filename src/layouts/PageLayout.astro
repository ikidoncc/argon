---
import Layout from './Layout.astro';

interface Props {
  title: string;
  description?: string;
}

const { title, description } = Astro.props;
---

<Layout title={`${title} | Argon`} description={description}>
  <div class="page-container container">
    <header class="page-header">
      <h1 class="page-title">{title}</h1>
      {description && <p class="page-desc">{description}</p>}
      <div class="page-divider"></div>
    </header>

    <div class="page-content">
      <slot />
    </div>
  </div>
</Layout>

<style>
  .page-container {
    padding-top: 4rem;
    padding-bottom: 6rem;
  }

  .page-header {
    margin-bottom: 3rem;
  }

  .page-title {
    font-size: 3rem;
    line-height: 1.1;
    letter-spacing: -0.04em;
    margin-bottom: 1rem;
    color: var(--color-text);
  }

  .page-desc {
    font-size: 1.25rem;
    color: var(--color-text-muted);
    line-height: 1.5;
    margin-bottom: 0;
  }

  .page-divider {
    height: 1px;
    background: var(--color-border);
    margin-top: 2rem;
  }

  .page-content {
    font-size: 1.05rem;
    line-height: 1.75;
  }

  @media (max-width: 600px) {
    .page-title {
      font-size: 2.25rem;
    }
    .page-desc {
      font-size: 1.1rem;
    }
  }
</style>
